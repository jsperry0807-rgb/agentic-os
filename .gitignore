# Agentic OS — gitignore

# OS junk
.DS_Store
Thumbs.db

# Wiki raw sources (user-managed, not versioned by default)
wiki/raw/*

# Local overrides
*.local

# Secrets — API keys, set by install.sh prompt
.env
